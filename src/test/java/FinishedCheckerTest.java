import static org.testng.Assert.assertEquals;
import static org.testng.Assert.assertTrue;

public class FinishedCheckerTest {
    // TODO uncomment and go on with TDD here
    /*final static int howManyInRowToWin = 3;
    private FinishedChecker finishedChecker;
    private BoardController gameBoard;

    @BeforeMethod
    public void setUp() {
        finishedChecker = new FinishedChecker(howManyInRowToWin);
    }

    @Test
    public void test() {
        // given
        // when
        gameBoard.tryMarkField(1, Symbol.X);
        gameBoard.tryMarkField(2, Symbol.X);
        gameBoard.tryMarkField(3, Symbol.X);
        // then
        boolean isWinHorizontally = finishedChecker.checkIsWinHorizontally();
        assertTrue(isWinHorizontally);
        assertEquals(gameBoard.getFinishedState(), FinishState.XWon);
    }*/

}
