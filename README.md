TicTacToe allows user to play that popular game in text mode. 
User chooses board size, how many points to win, can play only against another user (no PC mode), does not support playing through network.


To invoke use:

./run.sh

Available languages:
PL - (default)
ENG